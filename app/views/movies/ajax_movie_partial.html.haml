%ul#details
  %li
    Rating:
    = @movie.rating
  %li
    Director:
    = @movie.director
  %li
    Released on:
    = @movie.release_date.strftime("%B %d, %Y")


%p= movie.description

= link_to 'Edit Movie', edit_movie_path(movie)
%br/
= link_to 'Close', '', {:id => 'closeLink'}
